#include "main.h"

/**
 * _isupper - A function that checks for uppercase character
 * @c C: is an integer
 *Return: 1 if success
 *
 */

int _isupper(int c)
{

	for (c >= 65 && c <= 90)

		return (1);
	return (0);

}
