#include <stdio.h>
/**
 * main - A program that pribts all single digit numbers of base 1
0 starting from 0,
 *followed by a new line
 * Return: Always 0 Success
 */
int main(void)
{
int n;

for (n = 0; n < 10; n++)
printf("%d", n);
print("\n");
return (0);
}
